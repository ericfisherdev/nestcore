// Package components ships the Hearth shell's structural templ components:
// the parameterized Layout (264px sidebar plus main) and NavPill. Palette,
// PWA manifest wiring, and app-specific motion are deliberately NOT modeled
// here — see ShellProps's HeadExtras, SidebarBrand, SidebarExtra, and
// SidebarFooter slots, which are where each app injects that content.
package components
